//for off: ["","",""] and [{},{},{}]
/*
const arr=[1,2,3,4,5]
for (const num of arr) {
    console.log(num);
}

const greetings= "Hello World"
for (const greet of greetings) {
    console.log(`each char is ${greet}`);
}

//Maps: hold key value pair, collection of unique value pairs
const map= new Map()
map.set('IN','INDIA')
map.set('USA','AMERICA')
map.set('FR','FRANCE')
map.set('IN','INDIA')

//console.log(map)
for (const key of map) {
    console.log(key); //prints thw whole as array
}

for (const [key,value] of map) {
    console.log(key,":-",value); //prints key and value separately
}

//forof is not applicable for objects, so we use forin loop



const myObject={
    js:'JavaScript',
    cpp:'C++',
    rb:'Ruby',
    swift:"swift by apple"
}
for (const key in myObject) {
    console.log(`${key} shortcut is for ${myObject[key]}`);
}

//forof helps to print the keys or directly the values but forin only prints the keys
*/

//Most used loop: "FOREACH LOOP"
//do not have any function name parameter directly have the values
//it does not return any value


/*const coding= ["js","ruby","java","python","cpp"]
coding.forEach(  function (item) {
    console.log(item);
} )

//foreach in arrow function
coding.forEach( (item) => {
    console.log(item);
})

function printMe(item){
    console.log(item);
}
coding.forEach(printMe)


const myCoding=[
    {
        languageName:"javascript",
        languageFileName:"js"
    },
    {
        languageName:"java",
        languageFileName:"java"
    },
    {
        languageName:"python",
        languageFileName:"py"
    },
]
myCoding.forEach( (item)=> {
    console.log(item.languageName);
})
*/

